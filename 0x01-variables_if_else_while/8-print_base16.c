#include <stdio.h>
/**
  * main - Entry point
  * Return: 0
  */
int main(void)
{
char baseSixteen;

for (baseSixteen = 48; baseSixteen <= 57; baseSixteen++)
{
putchar(baseSixteen);
}
for (baseSixteen = 97; baseSixteen <= 102; baseSixteen++)
{
putchar(baseSixteen);
}
putchar ('\n');
return (0);
}
